# 주요기능 
1. 파일업로드
2. 업로드된 파일 리스트 트리구조로 표시 (상위폴더 포함)
3. 매뉴얼 레이아웃 섹션 별 패턴화기반 분석처리
4. 매뉴얼 레이아웃 섹션 별 AI를 선택적 활용을 통해 자연어 설명 생성
5. 생성된 매뉴얼 미리보기 기능
6. 생성된 매뉴얼은 html 또는 md파일로 다운로드 받을 수 있어야 함.
7. 업로드 및 생성된 매뉴얼 초기화 할 수 있는 기능 (초기 화면으로 돌아감)

# 매뉴얼생성 워크플로우 
1. 단일파일 / 멀티파일 / 폴더를 선택하여 업로드
- 업로드 가능 확장자는 .clx파일과 .js파일의 컴파일된 확장자인 .clx.js 확장자 파일.

2. 업로드 된 파일 중 매뉴얼을 생성하고자 하는 파일을 선택한다. 
- 트리구조로 상위폴더를 포함한 경로를 통해 표시 
- 전체선택 / 전체해제 / 경로단위선택 / 단건선택 

3. 파일을 선택한 후 '매뉴얼생성'버튼을 클릭하면 [분석결과] 페이지 / [생성매뉴얼] 페이지로 나뉘어 분석 및 생성정보가 표시된다.
- 탭 구조로 나뉘어 페이지를 선택할 수 있도록 함.
- [분석결과] 탭페이지에는 매뉴얼 생성을 위한 분석한 자료를 표시한다.
    > 화면개요
    > 사용방법
    > 참고사항
    > 항목
        >> 그리드 항목을 설명한다. (단, PatisTitleBar에 bindObject속성에 지정된 그리드만 해당된다.)
        >> INFOGROUP 항목을 설명한다. (단, GROUP의 스타일 속성 중 class속성이 'cl-form-group'인 GROUP만 해당된다.)
    > 탭페이지
        >> 화면 상의 tabfolder 컨트롤이 존재하는 경우 해당된다. 
        >> PatisUtils.loadEmbApp 함수를 통해 로드되는 페이지가 해당된다.     
    > 팝업 
        >> PatisUtils.openPopup 함수를 통해 로드되는 페이지가 해당된다.

- [생성매뉴얼] 탭페이지에는 생성된 매뉴얼을 미리보기 하는 기능이다.        

4. 'Html파일 다운로드'/ 'Md파일 다운로드'를 통해 생성된 매뉴얼을 다운로드 한다.

5. '매뉴얼초기화' 버튼을 눌러 진행된 모든 프로세스를 초기화 할 수 있다.

