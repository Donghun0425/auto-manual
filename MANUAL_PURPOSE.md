## 매뉴얼 레이아웃
### 화면개요
- 화면 스크립트 상단 주석을 통해 화면에 대한 설명을 3줄이내로 설명. (AI모델 활용)


### 사용방법
- PatisMenuTitleBar에 구성된 조회,신규,저장,삭제,추가버튼 등의 주요 기능을 3줄이내로 설명한다. 
    > 조회 : Form_Inq~ 와 같은 함수를 기준으로 판단한다. 
    > 신규 : Form_new~ 와 같은 함수를 기준으로 판단한다. 
    > 삭제 : Form_del~ 와 같은 함수를 기준으로 판단한다. 
    > 저장 : Form_save~ 와 같은 함수를 기준으로 판단한다. 
    > 추가버튼 : Form_ext~ 와 같은 함수를 기준으로 판단한다. (버튼 명칭은 해당 함수의 주석을 통해 추출한다.)

- PatisTitleBar에 구성된 조회,신규,저장,삭제,추가버튼 등의 주요 기능을 3줄이내로 설명한다.
    > 조회 : TitleForm_inq~ 와 같은 함수를 기준으로 판단한다. 
    > 신규 : TitleForm_new~ 와 같은 함수를 기준으로 판단한다. 
    > 삭제 : TitleForm_del~ 와 같은 함수를 기준으로 판단한다. 
    > 저장 : TitleForm_save~ 와 같은 함수를 기준으로 판단한다. 
    > 추가버튼 : TitleForm_ext~ 와 같은 함수를 기준으로 판단한다. (버튼 명칭은 App_initDataAfter 함수의 initAddButton을 통해 추출한다.)

- PatisMenuTtileBar, PatisTitleBar에서 제공되는 기능 외에 화면 내에 존재하는 버튼들의 주요 기능을 3줄이내로 설명한다.	

- 작성 규칙은 다음과 같다. (작성 규칙 설명 소제목을 {B}{/B}태그로 감싸고 그 하위 설명을 Step + 순번을 매겨 설명한다)
{B} 설명 기능 {/B}
Step1. 설명 1
Step2. 설명 2
Step3. 설명 3
Step4. 설명 4
....

예) {B}교과목정보 조회{/B}
	Step1.조회조건의 교과목코드, 교과목명, 이수구분 등을 선택하여 화면상단의 조회버튼을 클릭한다.

	{B}교과목정보 신규{/B}
	Step1.신규 입력시 초기설정을 위해 조회조건 중 년도, 학기를 선택한다. 
	Step2.

	{B}교과목정보 저장{/B}
	Step1.수정하고자 하는 자료를 입력 또는 선택한다.
	Step2.화면 상단의 '저장'버튼을 클릭하여 저장처리를 진행한다.

	{B}교과목정보 삭제{/B}
	Step1.삭제하고자 하는 자료를 선택한다.
	Step2.화면 상단의 '삭제'버튼을 클릭하여 삭제처리를 진행한다.
	Step3.삭제 시 '교육과정 자료가 존재합니다.' 메시지가 표시될 경우 입력된 교육과정 정보를 우선 삭제 처리해야한다.

	{B}전년도 복사{/B}
	Step1.복사하고자 하는 년도, 학기를 선택한다.
	Step2.복사 버튼을 클릭한다.
	Step3.복사 자료를 확인한다.

### 참고사항
- 조회 시 필수값을 설명한다.
- 저장 시 필수값을 설명한다.
- 삭제 시 전 처리 로직에 대해 설명한다.
- 여러 주요 기능 동작 전 처리해야 되는 로직이나, 예외가 발생하는 부분에 대해 설명한다.

- 작성 규칙 설명 소제목을 {B}{/B}태그로 감싸고 그 하위 설명을 순번을 매겨 설명한다.
{B}조회 시 필수선택 사항 {/B}
1.년도
2.학기

{B}저장 시 필수선택 사항 {/B}
1.년도
2.학기

등..